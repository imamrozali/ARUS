{
  "name": "arusjs",
  "version": "0.1.0",
  "private": true,
  "description": "ARUSJS — Explicit, linear, V8-optimized backend execution framework",
  "author": "Muhammad Imam Rozali <Nusa Innovation | muh.imamrozali@gmail.com>",
  "license": "MIT",
  "packageManager": "npm@9.12.0",
  "workspaces": [
    "packages/*",
    "benchmarks"
  ],
  "scripts": {
    "build": "npm run --workspace=@arusjs/core build && npm run --workspace=@arusjs/common build && npm run --workspace=@arusjs/adapter-express build && npm run --workspace=@arusjs/adapter-fetch build && npm run --workspace=@arusjs/adapter-http build && npm run --workspace=@arusjs/adapter-nest build && npm run --workspace=@arusjs/orm build",
    "dev": "npm run dev --workspaces",
    "clean": "npm run clean --workspaces",
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspaces",
    "bench": "npm --filter benchmarks start"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "ts-node": "^10.9.2",
    "tsup": "^8.5.1"
  }
}
